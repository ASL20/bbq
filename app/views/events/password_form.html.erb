<h1>Доступ только по пин-коду</h1>

<%= form_tag('', method: 'post') do |f| %>
  <div class="mb-3">
    <%= label_tag :pincode, 'Пин-код', class: 'form-label' %>
    <%= text_field_tag :pincode, params[:pincode], class: 'form-control' %>
  </div>

  <div class="actions">
    <%= submit_tag "Принять", class: 'btn btn-primary' %>
  </div>
<% end %>

<%= link_to 'На главную', events_path, class: 'nav-link' %>
